import { DateFormat1Pipe } from './date-format1.pipe';

describe('DateFormat1Pipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormat1Pipe();
    expect(pipe).toBeTruthy();
  });
});
